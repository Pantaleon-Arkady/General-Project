class UserManager {

    constructor() {

        this.user = '';


        // check in local storage if user exists
    }

    register(email, password) {
        // fjdkslfdl

        // set user to local storage
        this.user = ''; //
    }

    login(username, password) {
        // fetch post login
        this.user = '';
    }

    logout() {
        this.user = null;
        // clear local storage
    }

}


// const currentUserManager = new UserManager();



// currentUserManager.register(email, password);


// currentUserManager.login();